# 快速开始

[返回文档中心](README.md)

## 运行环境

| 要求 | 说明 |
| --- | --- |
| 系统 | Windows 10 / 11 优先。 |
| 浏览器 | Microsoft Edge。 |
| 网络 | 能访问辽宁大学图书馆座位预约系统。 |
| Python | 源码运行需要 Python；打包版用户不需要自己配置 Python。 |

## 方式一：运行打包版

1. 解压 `dist/LNU-LibSeat-v2.0.0.zip`。
2. 双击 `LNU-LibSeat.exe`。
3. 首次启动会弹出使用帮助，阅读后点击“我知道了”。
4. 填写账号、校区、房间、目标时间段。
5. 建议第一次勾选“测试模式”，点击开始，确认 API 能扫描出方案。
6. 取消“测试模式”后，再执行真实预约。

## 方式二：源码运行

在项目根目录执行：

```powershell
.\run.bat
```

`run.bat` 会创建 `env` 虚拟环境、安装依赖，并用 `pythonw.exe app.py` 启动 GUI。

已安装依赖时，也可以手动运行：

```powershell
env\Scripts\python.exe app.py
```

## 第一次应该怎么配置

| 区域 | 建议 |
| --- | --- |
| 账号 | 先填写第一个账号。单账号模式只使用第一个账号。 |
| 校区/房间 | 选择真实想预约的目标房间。 |
| 时间段 | 例如 `09:00-21:00`。程序会按当前时间和学校开放时间自动校正。 |
| 跨房间 | 新手可以先开启，并勾选愿意接受的房间。 |
| 优先座位 | 可不填；若填写，配合“优先座位优先”使用。 |
| 测试模式 | 第一次建议开启，不会执行真实预约。 |
| 单账号提前提醒 | 例如 `30` 分钟，表示当前预约结束前 30 分钟扫描下一段。 |
| 自动取消并换座 | 第一次不建议开启，先使用弹窗确认。 |

## 一个单账号逐段例子

目标：从 `09:00` 学到 `21:00`。

1. 选择“单账号逐段”。
2. 设置时间段 `09:00-21:00`。
3. 设置提前 `30` 分钟查找下一座位。
4. 程序先预约第一段，例如 `09:00-14:00`。
5. 到 `13:30` 时开始扫描 `14:00` 后的下一段。
6. 找到下一段后弹窗展示座位、房间、时间。
7. 点击确认后，程序取消当前预约并预约新座位。

## 注意

- 测试模式不会预约、取消或换座。
- 真实模式会提交预约和取消操作。
- 运行中尽量不要手动操作自动打开的浏览器。
- 关闭运行中的程序会停止本次计时，但单账号模式会保存恢复状态，下次启动后可选择恢复。

